http {
    server {
        listen <%= ENV["PORT"] %>;
        server_name localhost;
        root /app;  # Changed from '.' to '/app'

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /images/ {
            try_files $uri =404;
        }
    }
}
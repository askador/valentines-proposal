daemon off;
events {
    worker_connections 1024;
}

http {
    server {
        listen <%= ENV["PORT"] %>;
        server_name localhost;
        root /app;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /images/ {
            try_files $uri =404;
        }
    }
}